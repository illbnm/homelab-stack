services:
  gotify:
    image: gotify/server:2.3.1
    container_name: gotify
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - gotify-data:/app/data
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - GOTIFY_PASS=${GOTIFY_PASSWORD:?DOMAIN and GOTIFY_PASSWORD required}
      - GOTIFY_ADMIN_TOKEN=${GOTIFY_ADMIN_TOKEN:-}
      - GOTIFY_SERVER_PORT=8080
    labels:
      - traefik.enable=true
      - "traefik.http.routers.gotify.rule=Host(`gotify.${DOMAIN:?DOMAIN must be set}`)"
      - traefik.http.routers.gotify.entrypoints=websecure
      - traefik.http.routers.gotify.tls=true
      - traefik.http.services.gotify.loadbalancer.server.port=8080
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- --spider http://localhost:8080/ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  ntfy:
    image: binwiederhier/ntfy:v2.11.0
    container_name: ntfy
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - ntfy-data:/var/lib/ntfy
      - ntfy-cache:/var/cache/ntfy
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - NTFY_AUTH_BEARER_TOKEN=${NTFY_AUTH_TOKEN:-}
    command: serve
    labels:
      - traefik.enable=true
      - "traefik.http.routers.ntfy.rule=Host(`ntfy.${DOMAIN:?DOMAIN must be set}`)"
      - traefik.http.routers.ntfy.entrypoints=websecure
      - traefik.http.routers.ntfy.tls=true
      - traefik.http.services.ntfy.loadbalancer.server.port=80
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/v1/health?json=true || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  apprise:
    image: caronc/apprise:1.1.6
    container_name: apprise
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - apprise-config:/config
    environment:
      - TZ=${TZ:-Asia/Shanghai}
    labels:
      - traefik.enable=true
      - "traefik.http.routers.apprise.rule=Host(`apprise.${DOMAIN:?DOMAIN must be set}`)"
      - traefik.http.routers.apprise.entrypoints=websecure
      - traefik.http.routers.apprise.tls=true
      - traefik.http.services.apprise.loadbalancer.server.port=8000
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/ && test \"$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/)\" = '200' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

networks:
  proxy:
    external: true

volumes:
  gotify-data:
  ntfy-data:
  ntfy-cache:
  apprise-config:
