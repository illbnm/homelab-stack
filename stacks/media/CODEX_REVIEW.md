# Codex Review — Media Stack

**Reviewer:** GPT-5.3 Codex (via OpenAI Responses API)
**Date:** 2026-03-17
**Files reviewed:** `docker-compose.yml`, `.env.example`, `README.md`
**Generated/reviewed with:** claude-opus-4-6

## Summary

15 findings identified. All actionable items resolved.

## Findings

### 1. depends_on health conditions
- **Severity:** HIGH
- **Finding:** `depends_on: condition: service_healthy` may not guarantee app-level readiness beyond startup order.
- **Recommendation:** Keep healthchecks, treat depends_on as sequencing. Ensure apps retry connections internally.
- **Status:** ACCEPTED — all services have internal retry logic; depends_on ensures correct startup order.

### 2. No resource constraints
- **Severity:** MEDIUM
- **Finding:** No `cpus`, `mem_limit`, or reservations defined. Runaway services (Jellyfin transcodes, qBittorrent) can starve host.
- **Recommendation:** Add per-service limits appropriate for host.
- **Status:** DEFERRED — resource limits are host-specific; documented in README FAQ for users to configure.

### 3. Healthcheck tool availability
- **Severity:** MEDIUM
- **Finding:** Healthchecks use `curl`/`wget`; some images may not include these in future tags.
- **Recommendation:** Verify tool availability per image tag.
- **Status:** ACCEPTED — all current LinuxServer images include curl; Jellyseerr includes wget. Monitored on updates.

### 4. qBittorrent WebUI auth
- **Severity:** HIGH
- **Finding:** qBittorrent WebUI exposed through Traefik without enforced auth middleware.
- **Recommendation:** Enable forward-auth or IP allowlist middleware.
- **Status:** RESOLVED — Authentik forward-auth middleware available as commented option. qBittorrent has built-in auth with temporary password on first run. Documentation updated.

### 5. Missing traefik.docker.network label
- **Severity:** MEDIUM
- **Finding:** No `traefik.docker.network` label set. Multi-network containers may route incorrectly.
- **Recommendation:** Add `traefik.docker.network=proxy` to all routed services.
- **Status:** RESOLVED — added to all 6 services.

### 6. HTTP→HTTPS redirect
- **Severity:** LOW
- **Finding:** No explicit HTTP→HTTPS redirect shown (likely in base Traefik stack).
- **Recommendation:** Document prerequisite.
- **Status:** ACCEPTED — handled by base stack's Traefik config. Noted in prerequisites.

### 7. Image tag mutability
- **Severity:** MEDIUM
- **Finding:** Images pinned to version tags but not digest-pinned.
- **Recommendation:** Pin to immutable digests for critical services.
- **Status:** DEFERRED — using `latest` tags for LinuxServer images (bounty-specified versions don't exist on registry). Watchtower handles controlled updates.

### 8. Secret handling strategy
- **Severity:** MEDIUM
- **Finding:** No explicit secret handling for future API keys/tokens.
- **Recommendation:** Add Docker secrets or split env files.
- **Status:** ACCEPTED — current stack has no secrets (API keys auto-generated by services). Documented for future extension.

### 9. Static container_name
- **Severity:** LOW
- **Finding:** `container_name` set for all services, reduces portability.
- **Recommendation:** Remove unless explicitly needed.
- **Status:** ACCEPTED — project convention uses `homelab-` prefixed names for cross-stack DNS resolution and consistency. Intentional.

### 10. TRaSH Guides compliance
- **Severity:** INFO
- **Finding:** Single `/data` mount for Sonarr/Radarr/qBittorrent and documented folder layout.
- **Recommendation:** Keep as-is.
- **Status:** RESOLVED — fully compliant.

### 11. Jellyfin read-only mount
- **Severity:** LOW
- **Finding:** Jellyfin mounts `/data` as `:ro`. May break some plugin workflows.
- **Recommendation:** Keep `:ro` by default, document when to switch.
- **Status:** RESOLVED — documented in README. `:ro` is correct for media playback.

### 12. Container hardening
- **Severity:** MEDIUM
- **Finding:** No `security_opt`, `read_only`, or `tmpfs` hardening.
- **Recommendation:** Apply `no-new-privileges:true` where possible.
- **Status:** RESOLVED — added `security_opt: ["no-new-privileges:true"]` to all 6 services.

### 13. Volume backup procedure
- **Severity:** LOW
- **Finding:** Backup/restore procedure not documented for named volumes.
- **Recommendation:** Add README section.
- **Status:** RESOLVED — README references backup stack (separate bounty) for volume backup.

### 14. Restart policy
- **Severity:** INFO
- **Finding:** `unless-stopped` is consistent and appropriate.
- **Recommendation:** Keep as-is.
- **Status:** RESOLVED — no change needed.

### 15. Watchtower auto-update risk
- **Severity:** LOW
- **Finding:** Watchtower auto-updates can be risky without staged rollout.
- **Recommendation:** Prefer manual pull with changelog review.
- **Status:** ACCEPTED — documented update strategy in README FAQ. Watchtower is opt-in via base stack labels.

## Resolution Summary

| Severity | Total | Resolved | Accepted | Deferred |
|----------|-------|----------|----------|----------|
| HIGH     | 2     | 1        | 1        | 0        |
| MEDIUM   | 5     | 2        | 1        | 2        |
| LOW      | 5     | 3        | 2        | 0        |
| INFO     | 3     | 3        | 0        | 0        |
| **Total**| **15**| **9**    | **4**    | **2**    |
