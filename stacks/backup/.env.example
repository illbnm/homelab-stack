# =============================================================================
# HomeLab Stack — Backup & DR Environment Configuration
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# GENERAL
# -----------------------------------------------------------------------------
TZ=Asia/Shanghai
PUID=1000
PGID=1000
DOMAIN=yourdomain.com

# -----------------------------------------------------------------------------
# RESTIC BACKUP REPOSITORY
# -----------------------------------------------------------------------------
# Path where Restic REST Server stores its repository
RESTIC_REPO_PATH=./data/restic-repo
RESTIC_PORT=8100
# Restic encryption password — REQUIRED: openssl rand -base64 32
RESTIC_PASSWORD=
# Restic REST server URL (used by backup scripts)
BACKUP_RESTIC_REPO=http://restic-server:8000

# -----------------------------------------------------------------------------
# BACKUP TARGET
# Options: local | s3 | b2 | sftp | r2
# -----------------------------------------------------------------------------
BACKUP_TARGET=local

# Local backup directory
BACKUP_DATA_DIR=./data/backups

# -----------------------------------------------------------------------------
# S3 / MinIO TARGET
# -----------------------------------------------------------------------------
S3_ENDPOINT=                # e.g. https://minio.yourdomain.com
S3_BUCKET=homelab-backups
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1

# -----------------------------------------------------------------------------
# BACKBLAZE B2 TARGET
# -----------------------------------------------------------------------------
B2_ACCOUNT_ID=
B2_ACCOUNT_KEY=
B2_BUCKET=homelab-backups

# -----------------------------------------------------------------------------
# SFTP TARGET
# -----------------------------------------------------------------------------
SFTP_HOST=
SFTP_PORT=22
SFTP_USER=
SFTP_PATH=/backups/homelab
# SFTP key file path (inside container)
SFTP_KEY_FILE=

# -----------------------------------------------------------------------------
# CLOUDFLARE R2 TARGET (S3-compatible)
# -----------------------------------------------------------------------------
R2_ENDPOINT=                # e.g. https://<account_id>.r2.cloudflarestorage.com
R2_BUCKET=homelab-backups
R2_ACCESS_KEY=
R2_SECRET_KEY=

# -----------------------------------------------------------------------------
# BACKUP RETENTION
# -----------------------------------------------------------------------------
BACKUP_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# NOTIFICATIONS (ntfy)
# -----------------------------------------------------------------------------
NTFY_URL=http://ntfy:80
NTFY_TOPIC=homelab-backup
NTFY_TOKEN=                 # Optional: ntfy auth token

# -----------------------------------------------------------------------------
# STACKS TO BACKUP
# Comma-separated list of stack names, or "all"
# -----------------------------------------------------------------------------
BACKUP_STACKS=all
