# Disaster Recovery Guide — HomeLab Stack

Complete recovery procedure from total hardware failure.

Copyright (c) 2026 思捷娅科技 (SJYKJ) | License: MIT

---

## Recovery Order (RTO: ~2 hours)

The restore order matters due to service dependencies:

```
1. Base Infrastructure (Traefik + Portainer)    ~15 min
2. Databases (PostgreSQL + Redis + MariaDB)     ~10 min
3. SSO (Authentik)                              ~15 min
4. Backup Stack (Duplicati + Restic)            ~10 min
5. All other stacks                             ~30 min
6. Verify + health checks                       ~20 min
```

**Total estimated RTO: ~100 minutes**

---

## Step 1: Fresh Host Setup (~20 min)

```bash
# Install Docker + Compose
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER

# Clone the repo
git clone https://github.com/your-org/homelab-stack.git /opt/homelab
cd /opt/homelab

# Restore .env from backup
./scripts/backup.sh --restore latest --target backup
cp /opt/homelab/restored/backup/.env /opt/homelab/.env

# Create proxy network
docker network create proxy
```

## Step 2: Base Infrastructure (~15 min)

```bash
cd stacks/base
cp .env.example .env  # Edit with your domain, passwords

# Generate Traefik dashboard password
htpasswd -nbB admin 'yourpassword' | sed -e 's/\$/$$/g'

# Create ACME storage
touch ../../config/traefik/acme.json
chmod 600 ../../config/traefik/acme.json

docker compose up -d
docker compose ps  # Wait for all healthy
```

## Step 3: Databases (~10 min)

```bash
cd stacks/databases
docker compose up -d

# Restore database dumps
./scripts/backup.sh --restore <db-snapshot-id>
# Restore PostgreSQL
docker exec -i postgres pg_restore -U postgres < /restore/postgres.dump
# Restore MariaDB
docker exec -i mariadb mysql -u root < /restore/mariadb.dump
```

## Step 4: SSO (~15 min)

```bash
cd stacks/sso
docker compose up -d
# Wait for Authentik to initialize (~2 min)

# Restore Authentik database from backup
# SSO must be running before other stacks that depend on it
```

## Step 5: Backup Stack (~10 min)

```bash
cd stacks/backup
docker compose up -d

# Verify backup repository
./scripts/backup.sh --verify

# List available snapshots
./scripts/backup.sh --list
```

## Step 6: All Other Stacks (~30 min)

```bash
# Start each stack in order
for stack in storage network media productivity ai home-automation monitoring notifications; do
    cd /opt/homelab/stacks/$stack
    if [[ -f docker-compose.yml ]]; then
        docker compose up -d
        sleep 10
    fi
done

# Restore data volumes for each stack
./scripts/backup.sh --restore <snapshot-id>
```

## Step 7: Verify Everything (~20 min)

### Health Check All Services

```bash
# Check all containers are running
docker ps --format "table {{.Names}}\t{{.Status}}" | sort

# Check Traefik routing
curl -sk https://traefik.${DOMAIN}/ping

# Check all services respond
for svc in portainer backup restic; do
    curl -sk -o /dev/null -w "${svc}: %{http_code}\n" "https://${svc}.${DOMAIN}"
done
```

### Data Integrity Checklist

- [ ] All containers running and healthy
- [ ] Traefik dashboard accessible (BasicAuth works)
- [ ] Portainer shows all containers
- [ ] Database connections work (pg_isready, redis-cli ping)
- [ ] SSO login works
- [ ] File storage accessible (Nextcloud/MinIO)
- [ ] Media library intact (Jellyfin scan)
- [ ] Home Assistant dashboard loads
- [ ] Monitoring dashboards show data
- [ ] Backup verification passes

---

## Individual Stack Recovery

If only one stack needs recovery:

```bash
# Example: Restore media stack only
cd /opt/homelab
./scripts/backup.sh --list          # Find media snapshot
./scripts/backup.sh --restore <id>  # Restore it

cd stacks/media
docker compose up -d
```

## Emergency Contacts

| Service | Purpose | URL |
|---------|---------|-----|
| Traefik Dashboard | Check routing | `https://traefik.DOMAIN` |
| Portainer | Container management | `https://portainer.DOMAIN` |
| Duplicati | Backup management | `https://backup.DOMAIN` |

---

_Generated by 小米粒 (Xiaomili) - AI Agent | 思捷娅科技 (SJYKJ)_
