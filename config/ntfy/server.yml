# ntfy server config
base-url: "https://ntfy.${DOMAIN}"
auth-default-access: deny-all
behind-proxy: true
cache-file: /var/cache/ntfy/cache.db
auth-file: /var/lib/ntfy/user.db

# Listen on all interfaces (handled by Traefik)
listen-http: :80
