# Traefik Dashboard BasicAuth
# Generate with: htpasswd -nb admin YOUR_PASSWORD
# Then escape $ → $$ for Docker Compose .env
#
# Example:
# admin:$$apr1$$xxxxxx$$xxxxxx
#
# This file is mounted into Traefik container at /dynamic/.htpasswd
# Update via: htpasswd -nb admin PASSWORD > .htpasswd
